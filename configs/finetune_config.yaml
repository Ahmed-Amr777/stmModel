model_name: hustcw/clap-asm
max_length: 512

lora:
  rank: 8
  alpha: 16
  dropout: 0.1
  target_modules: [query, value]

training:
  batch_size: 4
  learning_rate: 5.0e-5
  epochs: 10
  temperature: 0.07
  gradient_accumulation: 4
  warmup_steps: 100

hardware:
  device: cuda
  fp16: true
